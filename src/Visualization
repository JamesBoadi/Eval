import React, { useEffect, useState } from "react";
import Box from './Box';

interface Props {
  previewPage: boolean;
  editPage: boolean;
}

export default function Visualization(props: Props) {
  const [_buttonOneClicked, setButtonOneClicked] = useState(false);

  let buttonOneClicked = (hide: boolean) => {
    console.log("hide button two");
  };

  let drawLine = () => {
   
  };

  let hideLine = () => {
    
  };

  /*
  Empty array acts like componentDidMount

  useEffect(() => {
    console.log("Effect");
  }, []); */

  useEffect(() => {
    if (_buttonOneClicked) buttonOneClicked(true);
    else buttonOneClicked(false);
  }, [_buttonOneClicked]);

  return (
    <>
      <button onClick={() => setButtonOneClicked(!buttonOneClicked)}>
        Draw Line
      </button>
      <button>Hide Line</button>
      <Box className="BoxOne"/>
      <Box className="BoxTwo"/>
    </>
  );
}
